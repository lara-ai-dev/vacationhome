import React from 'react'

//Title function for every page
export default function Title({title}) {
    return (
        <div className="section__title">
            <h4>{title}</h4>
            <div/>
        </div>
    )
}
